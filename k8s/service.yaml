apiVersion: v1
kind: Service
metadata:
  name: k8s-ai-agent
  namespace: k8s-ai-agent
spec:
  selector:
    app: k8s-ai-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
